
const useStyles = () => {
    return (
        {
            mainContainer: 'd-flex flex-column',
            langOption: 'd-flex flex-row align-items-center'
        }
    )
}

export default useStyles